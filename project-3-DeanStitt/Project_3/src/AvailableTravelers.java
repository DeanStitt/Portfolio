
public class AvailableTravelers extends Spaceship
{
	int travelers = 0;
	numOfTravelers [];
	availibleTravelers()
	{
		numOfTravelers [20];
		numOfTravelers = 0;
	}
	availibleTravelers(int travelers, numOfTravelers [])
	{
		numOfTravelers [] = new numOfTravelers [Capacity];
		numOfTravelers = 0;
	}
	public boolean numOfTraveler(Traveler)
	{
		boolean travelerFound = false;
		for (int i = 0; i < this.numberOfCrew; ++i)
		{
			if (this.crew[i].getId() == t.getId())
			{
				this.toString();
				public int addFlightToHistory(int flightHours) {
					return this.flightHours = this.flightHours + flightHours;
				}
				travelerFound = true;
				break;
			}
		}
		return travelerFound;
	}

	
}
